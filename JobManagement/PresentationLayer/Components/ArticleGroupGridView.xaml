<UserControl x:Class="PresentationLayer.Components.ArticleGroupGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.Components"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"  Style="{DynamicResource textblocktitle}">Article Groupe</TextBlock>
        <TreeView ItemsSource="{Binding ArticleGroups}" Grid.Row="1">
            <TreeView.Resources>
                <HierarchicalDataTemplate ItemsSource="{Binding SubordinateArticleGroups}"
                                          DataType="{Binding DataLayer.TransferObjects.ArticleGroup}"
                                  x:Key="articleGroupTemplate">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
